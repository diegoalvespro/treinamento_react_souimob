{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start", // iniciar o servidor na porta 3000
    "build": "react-scripts build", // gerar o build do projeto, quando estiver pronto para producao $: npm run build
    "test": "react-scripts test --env=jsdom", // rodar teste especifico para componente ou para o projeto 
    "eject": "react-scripts eject" 
  }
}


